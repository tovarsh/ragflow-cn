# 目标仓库
MIRROR_REGISTRY=ccr.ccs.tencentyun.com
MIRROR_NAMESPACE=comintern

# 输入清单（默认就是 upstream/images.json，可不填）
MIRROR_IMAGES_JSON=upstream/images.json

# multi-arch: system（建议先用） 或 all（全平台，非常慢）
MIRROR_MULTI_ARCH=all

# skopeo 重试次数
MIRROR_RETRY_TIMES=5

# 单镜像最大允许耗时（分钟）；超时会 kill 当前 docker 进程并继续下一个
MIRROR_COPY_TIMEOUT_MINUTES=90

# 是否把当前会话的 HTTP_PROXY/HTTPS_PROXY/NO_PROXY 传进容器（你有 Clash 就设 true）
MIRROR_USE_PROXY=true

# 可选：日志文件
MIRROR_LOG_FILE=mirror.log
